[package]
name = "models"
version = "1.0.0"
edition = "2021"
description = "Shared domain models and types with feature-gated functionality for postgres, AI, and testing"
readme = "README.md"

[features]
postgres = ["diesel", "pgvector", "chrono"]
testing = ["rand"]
ai = ["ollama-rs"]
vector-similarity = []
text-matching = []

[dependencies]
serde.workspace = true
schemars = "1.2"
diesel = { version = "^2.2.4", features = ["chrono"], optional = true }
pgvector = { version = "0.4.0", features = ["diesel"], optional = true }
ollama-rs = { workspace = true, optional = true }
rand = { version = "0.10", optional = true }
chrono = { version = "0.4", optional = true }
