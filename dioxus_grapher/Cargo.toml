[package]
name = "dioxus_grapher"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
dioxus = { workspace = true }

[dev-dependencies]
dioxus = { workspace = true, features = ["web"] }
